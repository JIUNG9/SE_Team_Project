<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 


<mapper namespace="SubjectMapper">
   

<insert id = "insert" parameterType = "SubjectDTO">
   INSERT INTO  Subject(Subject_Name, Professor_Name, std_Grade, Classroom, StartTime, EndTime, Maximum_Student)  VALUES(#{Subject_Name},#{Professor_Name}", #{std_Grade}, #{Classroom}, #{StartTime} ,#{EndTime}, #{Maximum_Student});

</insert>

<update id = "update" parameterType = "SubjectDTO" >

update Subject set
username = #{username}
password =#{password}
email = #{email}
bio = #{bio}
where id = #{id}



</update>

<delete id = "deleteById" parameterType = "int">
</delete>

<select id = "getAll" parameterType = "SubjectDTO">
   SELECT * FROM Subject; 
</select>




</mapper>